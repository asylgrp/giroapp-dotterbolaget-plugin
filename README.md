# giroapp-dotterbolaget-plugin

## Installation

Copy `dotterbolaget-plugin.php` to the plugins directory of your giroapp installation.

## Usage

```shell
giroapp ls --filter=dotterbolaget --format=dotterbolaget
```
